# Test environment configuration
NODE_ENV=test
PORT=5001
HOST=0.0.0.0

# QR code display for tests
QR_AUTH_ENABLED=false
QR_AUTH_SESSION_DURATION=86400000

# Test database
DB_PATH=:memory:

# Test Claude API key (mock)
CLAUDE_API_KEY=test-claude-api-key-for-testing

# Other test configurations
LOG_LEVEL=error
CORS_ORIGIN=*
TASK_TIMEOUT_MS=30000
MAX_CONCURRENT_TASKS=2
QUEUE_CONCURRENCY=1
WEBSOCKET_ENABLED=false
TUNNEL_TYPE=none
ENABLE_WORKTREE=false